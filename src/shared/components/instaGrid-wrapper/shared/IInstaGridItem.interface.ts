export interface IInstaGridItem {
  // Define the actual structure of your image data
  // For example: { uri: string; description: string; }
  uri: string;
  description: string;
  sourceId: string;
}
